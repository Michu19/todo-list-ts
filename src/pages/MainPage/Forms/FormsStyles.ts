import styled from 'styled-components';
import { myTheme } from '../../../styles/theme';

export const FormFlexCenter = styled.form`
display: ${myTheme.display.flex};
justify-content: ${myTheme.centerItems};
`
